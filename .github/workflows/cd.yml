name: CD Pipeline

on:
  push:
    branches:
      - main

jobs:

  # 1. Build & Push Docker Images
  # docker-build-push:
  #   name: Build & Push Docker Images
  #   runs-on: ubuntu-latest

  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v5

  #     - name: Set up Docker Buildx
  #       uses: docker/setup-buildx-action@v3

  #     - name: Log in to Docker Hub
  #       uses: docker/login-action@v3
  #       with:
  #         username: ${{ secrets.DOCKER_USERNAME }}
  #         password: ${{ secrets.DOCKER_PASSWORD }}

  #     # Backend
  #     - name: Build & push backend image
  #       uses: docker/build-push-action@v6
  #       with:
  #         context: ./backend
  #         file: ./backend/Dockerfile
  #         push: true
  #         tags: |
  #           ${{ secrets.DOCKER_USERNAME }}/rsb-backend:latest
  #           ${{ secrets.DOCKER_USERNAME }}/rsb-backend:${{ github.sha }}
  #         cache-from: type=gha
  #         cache-to: type=gha,mode=max

  #     # Frontend
  #     - name: Build & push frontend image
  #       uses: docker/build-push-action@v6
  #       with:
  #         context: ./frontend
  #         file: ./frontend/Dockerfile
  #         push: true
  #         tags: |
  #           ${{ secrets.DOCKER_USERNAME }}/rsb-frontend:latest
  #           ${{ secrets.DOCKER_USERNAME }}/rsb-frontend:${{ github.sha }}
  #         cache-from: type=gha
  #         cache-to: type=gha,mode=max

  #     # Admin
  #     - name: Build & push admin-dashboard image
  #       uses: docker/build-push-action@v6
  #       with:
  #         context: ./admin-dashboard
  #         file: ./admin-dashboard/Dockerfile
  #         push: true
  #         tags: |
  #           ${{ secrets.DOCKER_USERNAME }}/rsb-admin:latest
  #           ${{ secrets.DOCKER_USERNAME }}/rsb-admin:${{ github.sha }}
  #         cache-from: type=gha
  #         cache-to: type=gha,mode=max


  # 2. Deploy Frontend to Vercel
  deploy-frontend:
    name: Deploy Frontend to Vercel
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: frontend

    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Set up Node.js
        uses: actions/setup-node@v5
        with:
          node-version: '24'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Deploy Frontend to Vercel
        run: |
          rm -rf .vercel
          npx vercel@latest --prod --token=${{ secrets.VERCEL_TOKEN }}
        env:
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_FRONTEND_PROJECT_ID }}


  # 3. Deploy Admin Dashboard to Vercel
  # deploy-admin:
  #   name: Deploy Admin to Vercel
  #   runs-on: ubuntu-latest

  #   defaults:
  #     run:
  #       working-directory: ./admin-dashboard

  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v5

  #     - name: Set up Node.js
  #       uses: actions/setup-node@v5
  #       with:
  #         node-version: '24'
  #         cache: 'npm'
  #         cache-dependency-path: admin-dashboard/package-lock.json

  #     - name: Install dependencies
  #       run: npm ci

  #     - name: Build
  #       run: npm run build

  #     - name: Deploy Admin to Vercel
  #       run: |
  #         rm -rf .vercel
  #         npx vercel@latest --prod --token=${{ secrets.VERCEL_TOKEN }}
  #       env:
  #         VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
  #         VERCEL_PROJECT_ID: ${{ secrets.VERCEL_ADMIN_PROJECT_ID }}


  # 4. Deploy Backend to Render
  # deploy-backend:
  #   name: Deploy Backend to Render
  #   runs-on: ubuntu-latest

  #   steps:
  #     - name: Trigger Render deploy hook
  #       run: |
  #         curl --fail --silent --show-error \
  #           "${{ secrets.RENDER_DEPLOY_HOOK_URL }}" \
  #           -o /dev/null \
  #           -w "HTTP status: %{http_code}\n"